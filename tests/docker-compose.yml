version: '3.1'

services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_DB: mara
      POSTGRES_USER: mara
      POSTGRES_PASSWORD: mara
      POSTGRES_HOST_AUTH_METHOD: md5
    ports:
      - "5432"
